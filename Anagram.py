#import other scriptsd

#opening the word bank
word_list = open("eng_dict.txt")

#make hash table
#the keys will be alphabetized strings, the values will be lists of english words that math the keys
anagram_table = {}

#fill in hash table
#def fill_table():
    #for word in word_list:
    #alphabetize
    #make new key if not present
    #add to existing value if already in table
    
#filter the table for short anagram
#def filter_table_forlength(min_len):
    #for entry in table, remove if key length <min_len
    
#sort the hash table

#filter for suprising result
#patterns
#determine how far the letters moved 
